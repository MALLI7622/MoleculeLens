# MoleculeSTM Requirements
# Python 3.7 required
#
# STEP 1 - Install conda packages first:
#   conda install -y -c rdkit rdkit=2020.09.1.0
#   conda install -y -c conda-forge spacy
#
# STEP 2 - Install PyTorch with CUDA 11.1:
#   pip install torch==1.9.1+cu111 -f https://download.pytorch.org/whl/torch_stable.html
#
# STEP 3 - Install PyG extensions:
#   pip install torch-scatter==2.0.9 torch-sparse==0.6.12 torch-cluster==1.5.9 torch-spline-conv==1.2.1 torch-geometric==2.0.3 -f https://data.pyg.org/whl/torch-1.9.1+cu111.html
#
# STEP 4 - Install Megatron-LM (from local MolBART clone):
#   cd MolBART/megatron_molbart/Megatron-LM-v1.1.5-3D_parallelism && pip install .
#
# STEP 5 - Install apex (CUDA compilation required):
#   cd apex && pip install -v --disable-pip-version-check --no-cache-dir --no-build-isolation --global-option="--cpp_ext" --global-option="--cuda_ext" .
#
# STEP 6 - pip install -r requirements.txt
#
# RUNTIME NOTE: Add conda env lib to LD_LIBRARY_PATH for CUDA 11.1 libs:
#   export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"

# Core pip packages
requests==2.31.0
tqdm==4.67.3
matplotlib==3.5.3
Levenshtein==0.23.0
transformers==4.24.0
tokenizers==0.13.3
ogb==1.2.0
deepspeed==0.7.7
einops==0.6.1
yacs==0.1.8
rdflib==6.3.2
googledrivedownloader==0.4
scikit-learn==1.0.2
scipy==1.7.3
pandas==1.3.5
huggingface-hub==0.16.4
safetensors==0.3.0
ninja==1.11.1.4
pybind11==2.13.6
pydantic==1.10.13

# SciBERT / HuggingFace
boto3==1.33.13

# PySMILESutils (MolBART tokenizer)
PySMILESutils @ git+https://github.com/MolecularAI/pysmilesutils.git@b5b3b7d9755365d5a65de36693ef170c87db4e96

# MoleculeSTM itself — installed separately by setup_env.sh
